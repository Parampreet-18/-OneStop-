/* Global Styles */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

/* New Colour Palette and Background */
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #ffecd2, #fcb69f);
  color: #333;
  line-height: 1.6;
}

/* Dark Mode Background */
body.dark-mode {
  background: linear-gradient(135deg, #232526, #414345);
  color: #eee;
}

body.dark-mode header,
body.dark-mode footer {
  background-color: #1c1c1c;
}

/* Header */
header {
  background-color: #1a1a1a;
  color: #fff;
  padding: 1rem 0;
}

.header-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 1rem;
}

header h1 {
  margin-bottom: 0.5rem;
}

nav ul {
  list-style: none;
  display: flex;
  justify-content: center;
  margin-top: 0.5rem;
}

nav ul li {
  margin: 0 1rem;
}

nav ul li a {
  color: #ffecd2;
  text-decoration: none;
  font-weight: bold;
}

/* Header Buttons */
.header-buttons button {
  background-color: #ffecd2;
  color: #1a1a1a;
  border: none;
  padding: 0.5rem 1rem;
  border-radius: 4px;
  cursor: pointer;
}
.header-buttons button:hover {
  background-color: #fcb69f;
}

/* Main Content */
main {
  padding: 2rem;
}

section {
  margin-bottom: 2rem;
}

/* Controls */
.controls {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 1rem;
  margin-bottom: 1rem;
}

#searchBar,
#sortDropdown,
#categoryFilter {
  padding: 0.5rem;
  border: 1px solid #ccc;
  border-radius: 4px;
}

/* View Toggle Buttons */
.view-toggle button {
  padding: 0.5rem 1rem;
  border: 1px solid #ccc;
  background-color: #fff;
  cursor: pointer;
  border-radius: 4px;
}

.view-toggle button.active {
  background-color: #1a1a1a;
  color: #ffecd2;
  border-color: #1a1a1a;
}

/* Random Book Button */
#randomBookBtn {
  padding: 0.5rem 1rem;
  background-color: #fcb69f;
  color: #1a1a1a;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
#randomBookBtn:hover {
  background-color: #ffb07c;
}

/* Books Container */
#books-container {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: center;
}

.book {
  background-color: #fff;
  border: 1px solid #ddd;
  padding: 1rem;
  width: 200px;
  text-align: center;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  position: relative;
  cursor: pointer;
}

.book img {
  max-width: 100%;
  height: auto;
  margin-bottom: 0.5rem;
}

/* Favorite Icon */
.favorite-icon {
  position: absolute;
  top: 10px;
  right: 10px;
  font-size: 22px;
  color: #ccc;
  cursor: pointer;
}
.favorite-icon.favorited {
  color: #ff4d4d;
}

/* Rating Stars */
.rating-star {
  font-size: 24px;
  color: gold;
  cursor: pointer;
}
.rating-star.inactive {
  color: #ccc;
}

/* Submit Book Form */
#submitBookForm {
  max-width: 600px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
}
#submitBookForm label {
  margin-top: 1rem;
}
#submitBookForm input,
#submitBookForm textarea,
#submitBookForm select {
  padding: 0.5rem;
  border: 1px solid #ccc;
  border-radius: 4px;
  margin-top: 0.5rem;
}
#submitBookForm button {
  margin-top: 1rem;
  padding: 0.7rem;
  background-color: #1a1a1a;
  color: #ffecd2;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
#submitBookForm button:hover {
  background-color: #333;
}

/* Contact Form */
#contact form {
  max-width: 600px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
}
#contact form label {
  margin-top: 1rem;
}
#contact form input,
#contact form textarea {
  padding: 0.5rem;
  border: 1px solid #ccc;
  border-radius: 4px;
  margin-top: 0.5rem;
}
#contact form button {
  margin-top: 1rem;
  padding: 0.7rem;
  background-color: #1a1a1a;
  color: #ffecd2;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
#contact form button:hover {
  background-color: #333;
}

/* Footer */
footer {
  text-align: center;
  padding: 1rem;
  background-color: #1a1a1a;
  color: #ffecd2;
  position: fixed;
  bottom: 0;
  width: 100%;
}

/* Scroll-to-Top Button */
#scrollTopBtn {
  display: none;
  position: fixed;
  bottom: 70px;
  right: 20px;
  z-index: 100;
  background-color: #1a1a1a;
  color: #ffecd2;
  border: none;
  padding: 0.5rem 0.8rem;
  border-radius: 50%;
  font-size: 18px;
  cursor: pointer;
}
#scrollTopBtn:hover {
  background-color: #333;
}

/* Modal Styles */
.modal {
  display: none;
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  overflow: auto;
  background-color: rgba(0, 0, 0, 0.7);
}
.modal-content {
  background-color: #fff;
  margin: 5% auto;
  padding: 1rem;
  border: 1px solid #888;
  width: 90%;
  max-width: 600px;
  border-radius: 8px;
  text-align: center;
  position: relative;
}
.modal-content img {
  max-width: 100%;
  height: auto;
  margin-bottom: 1rem;
}
.close-button {
  position: absolute;
  right: 10px;
  top: 10px;
  font-size: 28px;
  font-weight: bold;
  cursor: pointer;
}
body.dark-mode .modal-content {
  background-color: #333;
  color: #eee;
}
